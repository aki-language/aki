# Premake5 example project

This project demonstrates how to use [Premake5](https://premake.github.io/) to generate project files from a codebase that contains bits in both Aki and C++.

It uses the extension from [Aki](../../../tooling/build/premake5) to automatically generate the c headers from aki files.
